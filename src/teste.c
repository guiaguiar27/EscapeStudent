#include <stdio.h> 
#include <stdlib.h> 
#include <time.h>

int move_student(int n, int *tentatives, ){ 
    return 0; 
}

void main(){ 
    int n ; // size     
    int **Maze=  (int**)malloc(n*sizeof(int*)); 
    for(int i = 0 ; i < n ; i++){ 
        Maze[i] = (int*)malloc(n*sizeof(int)); 
    }  

}